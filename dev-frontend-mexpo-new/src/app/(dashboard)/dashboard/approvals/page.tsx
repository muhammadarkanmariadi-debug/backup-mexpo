import ApprovalQueue from "@/features/dashboard/approvals/ApprovalQueue";

export const metadata = {
  title: "Persetujuan Publikasi",
};

export default function ApprovalsPage() {
  return <ApprovalQueue />;
}
